language: python
python:
 - '2.7'
 - '3.5'
install:
 - pip install .
 - pip install coveralls
# - pip install -r requirements.txt
script:
 - nosetests --with-coverage --cover-package=PerfectNS
after_success:
 - coveralls
